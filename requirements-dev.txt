# Test/lint dependencies. Works on any OS without robot hardware.
paho-mqtt>=2.0,<3
py-trees>=2.2,<3
pyserial>=3.5
httpx>=0.27
python-dotenv>=1.0
# no numpy<2 pin here: mediapipe (which needs it) is not installed for tests; any Python 3.9+ works
numpy
opencv-python-headless>=4.8,<5
pytest>=8
ruff>=0.6
